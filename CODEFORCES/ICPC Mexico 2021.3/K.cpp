#include <bits/stdc++.h>

using namespace std;

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);

	int T, D, M;
	cin >> T >> D >> M;

	int y[M];

	for (int& i : y)
		cin >> i;

	bool en = false;

	cout << (en ? "Y" : "N");
}