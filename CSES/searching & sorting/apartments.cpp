#include <bits/stdc++.h>

using namespace std;

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);

	int n, m, k, sum;
	cin >> n >> m>> k;

	int a[n], s[n];

	for(int i = 0; i < n; i++)
		cin >> a[i];

	for(int i = 0; i < m; i++)
		cin >> s[i];

	cout << sum;
}